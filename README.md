# albums
Albums App

## New repo setup
- Clone repository 
- Since this repository uses Jetpack compose you'll need to have a Canary build of Android Studio

## Usage
- Clone this repo to your local machine
- Open up the project on Android studio
- Sync the gradle files and download all the required libraries
- Run the app
- If there is network then the data is downloaded from the server otherwise it is downloaded from the local Room database.

## Future improvements
- Use all the latest technologies suggested by Android
- Replace RxJava with Coroutines
- Implement Android navigation components
- Create a dynamic module and move the Albums service into the dynamic module which gets installed on demand.
- Configure SonarCloud and achieve 100% code coverage through Unit tests and instrumentation tests.
